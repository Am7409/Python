def insertion(l):
    for i in range(1,len(l)):
        j=i
        while j>0 and l[j]<l[j-1]:
            (l[j],l[j-1])=(l[j-1],l[j])
            j-=1
    
    
l=[5,6,3,8,2,1]
insertion(l)
print(l)
